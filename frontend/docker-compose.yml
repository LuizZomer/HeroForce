version: "3.9"

services:
  frontend:
    build: .
    container_name: react_app
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    env_file:
      - .env
